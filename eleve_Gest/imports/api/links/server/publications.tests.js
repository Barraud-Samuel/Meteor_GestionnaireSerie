// Tests for the links publications
//
// https://guide.meteor.com/testing.html

/*
import { assert } from 'meteor/practicalmeteor:chai';
import { Eleves } from '../eleves.js';
import { PublicationCollector } from 'meteor/johanbrook:publication-collector';
import './publications.js';

describe('eleves publications', function () {
  beforeEach(function () {
    Eleves.remove({});
    Eleves.insert({
      title: 'meteor homepage',
      url: 'https://www.meteor.com',
    });
  });

  describe('eleves.all', function () {
    it('sends all eleves', function (done) {
      const collector = new PublicationCollector();
      collector.collect('eleves.all', (collections) => {
        assert.equal(collections.eleves.length, 1);
        done();
      });
    });
  });
});
*/
